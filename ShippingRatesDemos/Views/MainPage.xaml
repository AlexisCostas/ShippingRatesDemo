<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ShippingRatesDemos.Pages.MainPage"
             xmlns:resx="clr-namespace:ShippingRatesDemos.Resources.Translates"
             Title="{x:Static resx:AppResources.AppTitle}">
    <ContentPage.Content>
        <VerticalStackLayout Padding="30" Spacing="20">
            <Label Text="{x:Static resx:AppResources.AppDesc}" />
            <Button Text="{x:Static resx:AppResources.BtnCreateAddress}"
            Command="{Binding GoToCreateAddressCommand}" />
            <Button Text="{x:Static resx:AppResources.BtnRates}"
            Command="{Binding GoToRatesCommand}"
            IsEnabled="{Binding HasAddress}" />
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
